{{!< default}}
{{!-- The tag above means: insert everything in this file into the body of the default.hbs template --}}

{{#post}}

<main class="gh-main">

    <article class="gh-article {{post_class}}">

        <header class="gh-article-header gh-canvas">

            {{#if primary_tag}}
                <a class="gh-article-tag" href="{{primary_tag.url}}">{{primary_tag.name}}</a>
            {{/if}}
            <h1 class="gh-article-title is-title">{{title}}</h1>
            {{#if custom_excerpt}}
                <p class="gh-article-excerpt is-body">{{custom_excerpt}}</p>
            {{/if}}

            {{#if @custom.show_post_metadata}}
                <div class="gh-meta-share">
                    <div class="gh-article-meta">
                        <div class="gh-article-author-image instapaper_ignore">
                            {{#foreach authors}}
                                {{#if profile_image}}
                                    <a href="{{url}}">
                                        <img class="author-profile-image" src="{{img_url profile_image size="xs"}}" alt="{{name}}">
                                    </a>
                                {{else}}
                                    <a href="{{url}}">{{> "icons/avatar"}}</a>
                                {{/if}}
                            {{/foreach}}
                        </div>
                        <div class="gh-article-meta-wrapper">
                            <h4 class="gh-article-author-name">{{authors}}</h4>
                            <div class="gh-article-meta-content">
                                <time class="gh-article-meta-date" datetime="{{date format="YYYY-MM-DD"}}">{{date format="DD MMM YYYY"}}</time>
                                {{#if reading_time}}
                                    <span class="gh-article-meta-length"><span class="bull">—</span> {{reading_time minute=(t "1 min read") minutes=(t "% min read")}}</span>
                                {{/if}}
                            </div>
                        </div>
                    </div>
                    <a href="#/share" class="gh-button gh-button-share">
                        {{t "Share"}}
                    </a>
                </div>
            {{/if}}

            {{> "feature-image"}}

        </header>

        <section class="gh-content gh-canvas is-body{{#if @custom.enable_drop_caps_on_posts}} drop-cap{{/if}}">
            {{content}}
        </section>

    </article>

    {{#if comments}}
        <div class="gh-comments gh-canvas">
            {{comments}}
        </div>
    {{/if}}

</main>

    {{#if @custom.show_related_articles}}
        {{!-- 自主扩展功能：按标签智能匹配相关文章，提升内容发现能力 --}}
        {{#get "posts" include="authors,tags" filter="tags:[{{primary_tag.slug}}]+id:-{{id}}" limit="3" as |related_by_tag|}}
            {{#if related_by_tag}}
                <section class="gh-container is-grid gh-outer oss-related-section">
                    <div class="gh-container-inner gh-inner">
                        <h2 class="gh-container-title oss-related-title">
                            <span class="oss-related-icon">📚</span>
                            相关推荐
                            {{#if primary_tag}}
                                <span class="oss-related-tag">来自「{{primary_tag.name}}」</span>
                            {{/if}}
                        </h2>
                        <div class="gh-feed oss-related-feed">
                            {{#foreach related_by_tag}}
                                {{> "post-card" lazyLoad=true}}
                            {{/foreach}}
                        </div>
                    </div>
                </section>
            {{else}}
                {{!-- 降级方案：同标签文章不足时显示最新文章 --}}
                {{#get "posts" include="authors" filter="id:-{{id}}" limit="3" as |latest_posts|}}
                    {{#if latest_posts}}
                        <section class="gh-container is-grid gh-outer oss-related-section">
                            <div class="gh-container-inner gh-inner">
                                <h2 class="gh-container-title oss-related-title">
                                    <span class="oss-related-icon">📖</span>
                                    最新文章
                                </h2>
                                <div class="gh-feed oss-related-feed">
                                    {{#foreach latest_posts}}
                                        {{> "post-card" lazyLoad=true}}
                                    {{/foreach}}
                                </div>
                            </div>
                        </section>
                    {{/if}}
                {{/get}}
            {{/if}}
        {{/get}}
    {{/if}}
{{/post}}
